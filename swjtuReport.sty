\ProvidesPackage{swjtuReport}

% 页边距
\RequirePackage[left=2.50cm, right=2.50cm, top=2.50cm, bottom=2.50cm]{geometry}

% 导入一些必要的宏包，用户可以根据需要自行添加
\RequirePackage{latexsym,amssymb,amsmath,amsbsy,amsopn,amstext,amsthm,amsxtra,color,bm,calc,ifpdf}
\RequirePackage{graphicx}
\RequirePackage{enumerate}
\RequirePackage{fancyhdr}
\RequirePackage{listings}
\RequirePackage{multirow}
\RequirePackage{makeidx}
\RequirePackage{xcolor}
\RequirePackage{fontspec}  % 字体选择的宏包
\RequirePackage{setspace}  % 导入 setspace 宏包
\RequirePackage{subfigure}
\RequirePackage{hyperref}
\RequirePackage{pythonhighlight}
\RequirePackage{tcolorbox}
\RequirePackage{fontawesome}
\RequirePackage{caption}
\RequirePackage{cleveref}
\RequirePackage{etoolbox}
\RequirePackage{natbib}
\RequirePackage{booktabs} % 如果需要三线表
\RequirePackage{array} % 引入 array 包以便调整列对齐方式

% 设置中英文字体
\setmainfont{Times New Roman}[
    BoldFont = {Times New Roman Bold},
    ItalicFont = {Times New Roman Italic},
    BoldItalicFont = {Times New Roman Bold Italic}
]
\setCJKmainfont{Noto Serif CJK SC}[
    BoldFont = {Noto Serif CJK SC},
    ItalicFont = {Noto Serif CJK SC},
    AutoFakeBold = true,
    AutoFakeSlant = true
]

% 段落设置
\setlength{\parindent}{2em}  % 设置段落首行缩进为2个字母宽度
% 行距设置
% 解释行距设置
% 一般的 LaTeX 默认行距为 12 磅（约为单倍行距）
% 要设置为 20 磅，比例大约为 20/12 = 1.655
\setstretch{1.655}     % 设置行距为 20 磅

% 去掉图表标题中的冒号，将分隔变为空格
\captionsetup[figure]{labelsep=space}
\captionsetup[table]{labelsep=space}
% 
% 自己创建了一个字号 小五
\DeclareCaptionFont{customfont}{\fontsize{10.5}{12}\selectfont}
\captionsetup[table]{font=customfont}
\captionsetup[figure]{font=customfont}
% \captionsetup{labelformat=default,labelsep=space} % 去除冒号

\numberwithin{equation}{section} % 公式按章节编号
\numberwithin{figure}{section}   % 图按章节编号
\numberwithin{table}{section}    % 表按章节编号

% 更改摘要的标题的字体大小、清除\abstract环境的自动缩进
\RequirePackage{abstract}
\renewcommand{\abstractname}{\textbf{\large {摘\quad 要}}}
\setlength{\absleftindent}{0pt}
\setlength{\absrightindent}{0pt}

% 可固定下划线长度
\makeatletter
\newcommand\dlmu[2][4cm]{\hskip1pt\underline{\hb@xt@ #1{\hss#2\hss}}\hskip3pt}
\makeatother

% 更改引文、交叉引用、链接的颜色
\hypersetup{colorlinks=true,filecolor=black,linkcolor=black,urlcolor=black,citecolor=black}

% 对一些autoref的中文引用名作修改
\def\figureautorefname{图}
\def\tableautorefname{表}
\def\equationautorefname{式}
\def\footnoteautorefname{脚注}
\def\partautorefname{篇}
\def\chapterautorefname{章}
\def\sectionautorefname{节}
\def\subsectionautorefname{小节}
\def\subsubsectionautorefname{小小节}
\def\paragraphautorefname{段落}
\def\subparagraphautorefname{子段落}
\def\FancyVerbLineautorefname{行}
\def\itemautorefname{项}
\def\appendixautorefname{附录}

\newtheorem{Theorem}{定理}[section]
\newtheorem{Lemma}[Theorem]{引理}
\newtheorem{Corollary}[Theorem]{推论}
\newtheorem{Proposition}[Theorem]{命题}
\newtheorem{Definition}[Theorem]{定义}
\newtheorem{Example}[Theorem]{例}

\crefname{Theorem}{定理}{定理}
\crefname{Lemma}{引理}{引理}
\crefname{Corollary}{推论}{推论}
\crefname{Proposition}{命题}{命题}
\crefname{Definition}{定义}{定义}
\crefname{Example}{例}{例}


% 参考文献设置

% 重定义 \cite 命令为手动调整位置的上标带方括号，使其在数学环境中也能正确显示
\renewcommand{\cite}[1]{\textsuperscript{\text{[\citenum{#1}]}}}

\newcommand{\reference}{
    \bibliographystyle{unsrt}    % 规定了参考文献的格式，建议使用plain,plainnat,abbrvnat内置格式，也可以自制样式.bst文件来导入使用
    \bibliography{references}    % 调出LaTeX生成参考文献列表
    %\setcitestyle{open={(},close={)}}
}

% 自定义颜色
\definecolor{mygray}{rgb}{0.95, 0.95, 0.95}
\definecolor{mygreen}{rgb}{0, 0.6, 0}
\definecolor{myblue}{rgb}{0, 0, 0.8}
\definecolor{myred}{rgb}{0.8, 0, 0}
% 文本框设置
\newcommand{\tbox}[1]{
\begin{center}
\begin{tcolorbox}[colback=mygray,         % 灰色背景
                  colframe=black,          % 黑色边框
                  arc=1mm,                 % 圆角设置
                  boxrule=0.5pt,           % 边框线宽
                  width=\linewidth,        % 设置宽度为当前行宽
                  boxsep=5pt,              % 内边距
                  left=2pt, right=0pt,     % 左右内边距
                 ]
#1 % 文本框内容
\end{tcolorbox}
\end{center}
}
% 浅灰色边框
\tcbset{
    colframe=black,            % 边框颜色
    colback=mygray,             % 背景颜色
    coltitle=black,
    boxrule=0.8pt               % 边框线的粗细
}
% 定义Python代码的样式
\lstdefinestyle{mystyle}{
    backgroundcolor=\color{mygray},      % 设置背景颜色
    commentstyle=\color{mygreen},        % 注释的颜色
    keywordstyle=\color{myblue},         % 关键词的颜色
    numberstyle=\tiny\color{gray},       % 行号的颜色和大小
    stringstyle=\color{myred},           % 字符串的颜色
    basicstyle=\ttfamily\footnotesize,   % 代码的基本样式
    breaklines=true,                     % 自动换行
    frame=lines,                         % 代码块周围加上边框线
    numbers=left,                        % 行号显示在左侧
    numbersep=5pt,                       % 行号和代码的间距
}
\lstset{style=mystyle}


% 页眉设置，若某处留空则不显示
\pagestyle{fancy}
\fancyhead[L]{西南交通大学}
\fancyhead[C]{\fangsong 你的课程报告题目}
\fancyhead[R]{课程报告}

% 封面页设置
{   
    %标题
    \title{ 
    \vspace{2cm} 
    \heiti \Huge \textbf{{XXXX课程报告}} \par
    \vspace{1cm} 
    \heiti \Large {\underline{XXXXXX进展调研}}    
    \vspace{3cm} 
        }
    %信息
    \author{
    \vspace{0.5cm}
    \kaishu\Large 学院\ \dlmu[9cm]{XXXXXX学院} \qquad \\ %学院
    \vspace{0.5cm}
    \kaishu\Large 专业\ \dlmu[9cm]{XXXXXX专业} \qquad  \\ %专业
    \vspace{0.5cm}
    \kaishu\Large 学号\ \dlmu[9cm]{123456789}  \qquad  \\ %学号
    \vspace{0.5cm}
    \kaishu\Large 姓名\ \dlmu[9cm]{XXX} \qquad  \\ %姓名
        }
    %日期    
    \date{\today} 
}

% 封面logo设置，logo图片文件（建议以.pdf矢量图形式）放在figures文件夹里
\newcommand{\cover}{
    \begin{figure}
	\centering
        \vspace{2cm}
	\includegraphics[width=0.6\textwidth]{figures/swjtu_logo1.pdf}
    \end{figure} 
\maketitle
}

% 目录设置，为目录中的part、chapter、section补上引导点，注意：book或report类型中才有part和chapter，article没有
\RequirePackage[subfigure]{tocloft}
%\renewcommand{\cftpartleader}{\cftdotfill{\cftdotsep}}
%\renewcommand{\cftchapleader}{\cftdotfill{\cftdotsep}}
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}}